{
  "lock": false,
  "exclude": ["CHANGELOG.md"],
  "tasks": {
    "test": "deno test -R",
    "bench": "deno bench",
    "update-deps": "deno run -A 'https://deno.land/x/nudd@v0.2.11/cli.ts' update mod.ts deno.json test.ts, bench/*.ts"
  },
  "lint": {
    "rules": {
      "exclude": ["no-import-prefix"]
    }
  }
}
